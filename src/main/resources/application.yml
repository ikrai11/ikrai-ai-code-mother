spring:
  application:
    name: ikrai-ai-code-mother-backed
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.aistd.ikraiaicodemother.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
